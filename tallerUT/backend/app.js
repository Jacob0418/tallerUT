/*import express from 'express';
import morgan from 'morgan';
import authRoutes from './routes/auth.routes.js' 

const apps = express();

apps.use(morgan('dev'));

apps.use(express.json());

apps.use("/api",authRoutes);

export default apps; */